-- After importing the datasets, create the following indexes
create primary index on `dblpdb1` using gsi;

create primary index on `dblpdb2` using gsi;

create primary index on `dblpdb4` using gsi;

create primary index on `dblpdb8` using gsi;